video:
  source: "camera"  # "video" hoặc "camera"
  input_path: "data/input_video.mp4"
  camera_id: 0
  output_dir: "data/output"
  frame_width: 640
  fps: 20.0
detector:
  min_contour_area: 300
  blur_size: 11
  threshold_value: 30
  dilate_iterations: 1
log:
  log_dir: "logs"